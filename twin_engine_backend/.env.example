# ============================================================
# TwinEngine Backend — Environment Variables
# ============================================================
# Copy this file to .env and fill in values for your environment.
#   cp .env.example .env
# ============================================================

# -----------------------------------------------------------
# Django Core
# -----------------------------------------------------------
DEBUG=True
SECRET_KEY=django-insecure-dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------
# Database (SQLite is default for development)
# When ready for production, switch to PostgreSQL:
#   DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/twinengine?sslmode=require
# -----------------------------------------------------------
DATABASE_URL=sqlite:///db.sqlite3

# -----------------------------------------------------------
# CORS — Allowed frontend origins (comma-separated)
# -----------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# -----------------------------------------------------------
# CSRF — Trusted origins (comma-separated, usually same as CORS)
# -----------------------------------------------------------
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# -----------------------------------------------------------
# Redis — Used by Django Channels for WebSocket layer
# Leave empty to use InMemoryChannelLayer (dev only)
# -----------------------------------------------------------
REDIS_URL=

# -----------------------------------------------------------
# Azure OpenAI (for GPT-4o report generation)
# -----------------------------------------------------------
AZURE_OPENAI_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# -----------------------------------------------------------
# Cloudinary (media storage)
# -----------------------------------------------------------
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# -----------------------------------------------------------
# Email (SMTP — for notifications, optional)
# -----------------------------------------------------------
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True

# -----------------------------------------------------------
# Sentry (error monitoring, optional)
# -----------------------------------------------------------
SENTRY_DSN=

# -----------------------------------------------------------
# Logging
# -----------------------------------------------------------
LOG_LEVEL=DEBUG
